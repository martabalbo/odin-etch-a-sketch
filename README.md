A project from the Foundations course of The Odin Project.

The assignment is to generate a square grid, letting the user input the dimension, and make each square of the grid change colors when a cursor enters it.
As per the extra credit task, I wrote a function that chooses the square's color at random.
